{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "mvn -ntp -q -DskipTests dependency:resolve dependency:resolve-plugins test-compile >/dev/null 2>&1 || true"
          }
        ]
      }
    ]
  }
}
